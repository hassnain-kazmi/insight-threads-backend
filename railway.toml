[deploy]
releaseCommand = "alembic upgrade head"
# Healthcheck omitted: same repo used by backend (has /health) and Celery worker (no HTTP).
# Re-enable in Railway UI for the backend service only if the platform allows per-service overrides.
restartPolicyType = "ON_FAILURE"
